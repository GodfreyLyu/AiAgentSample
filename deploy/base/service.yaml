apiVersion: v1
kind: Service
metadata:
  name: ai-agent-sample
  labels:
    app.kubernetes.io/name: ai-agent-sample
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-agent-sample
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
      appProtocol: http
